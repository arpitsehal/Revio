{
  "name": "revio-desktop",
  "version": "1.0.0",
  "description": "Revio - Native High-Performance File Recovery & Versioning",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run tauri dev",
    "build": "cd frontend && npm run tauri build",
    "install:all": "cd frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
